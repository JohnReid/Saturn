#!/bin/bash -e

#
# Take the output of bams-list and submit a wellington job
#

while IFS='' read -r line || [[ -n "$line" ]]; do
    echo $line
    a=( $line )
    qsub SLURM/wellington.slurm ${a[1]} ${a[2]} ${a[3]} conservative
    qsub SLURM/wellington.slurm ${a[1]} ${a[2]} ${a[3]} relaxed
done
